_symlinks_
$ ln -s /thing/to/which/im/linking /new/symlink

_unzip
```shell
$ unzip zipped_file.zip
```

_open
```shell
xdg-open file.mp3
```

list of groups, members
```/etc/groups```

format usb:
make sure its unmounted
```umount /dev/<usb>```
use mkfs:
```sudo mkfs.vfat /dev/<usb>```

list dirs
ls -d */

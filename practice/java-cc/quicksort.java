package com.company;
import java.util.Arrays;
import java.util.*;

public class QuickSort {
    public static int sorter(int[] unsortedArray) {
				int pivot = unsortedArray[unsortedArray.length - 1];
				int[] left = [];
				int[] right = [];
				for (int i = 0; i < unsortedArray.length; i += 1) {
					if (unsortedArray[i] <= pivot) {

					}	
				}
        return pivot;
    }

    public static void main(String[] args) {
        int[] cool = {1,2,3,4};
        System.out.println(sorter(cool));
    }
}

// All numbers in JS are depicted as floats

var floatValue = 3.14;

// the methods below do what you would expect them to.
var intRound1 = Math.floor( floatValue );
var intRound2 = Math.ceil( floatValue );
var intRound3 = Math.round( floatValue );


def pad(s, n):
    print("need this dude")

def add_big_numbers(n1, n2):
    # seems like we have to do the padding, since popping an empty list throws
    # an error
    n1 = list(n1)
    n2 = list(n2)
    carry = 0
    sum = ""
    while n1 or n2 or carry > 0:
        




#include <stdio.h>

void mostCommonDataTypes(void)
{
	char		c; // stores a single character and requires a single byte of memory
	int			i; // stores an integer, typically 4 bytes
	float		f; // stores a decimal number with single precision
	double	d; // stores a decimal number with double precision
}

void allDataTypes(void)
{
	short int si = 32000;
	unsigned short int usi = 64000;
	unsigned int ui = 100;
	int i = 111;
	long int li = 111; // this is the same as int
	unsigned long int uli = 101010;

}

int main(int argc, char *argv[])
{
	
	return 0;
}

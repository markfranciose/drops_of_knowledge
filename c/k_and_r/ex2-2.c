/* write a loop equivalent to the loop above, without `&&` or `||` */

/* loop in question:
 * for(i=0; i<lim-1 && (c=getchar()) != '\n' && c != EOF; ++i)
      s[i] = c;
 */

#include <stdio.h>

int main(void)
{

  
}
